async {{findByIdMethod.methodName}}(id) {
    const request = {
        TableName: "{{tableName}}",
        Key: {
            "{{findByIdMethod.keyColumnName}}": id
        }
    };
    const promise = await dynamoDbClient.get(request).promise();
    return promise.Items;
}
